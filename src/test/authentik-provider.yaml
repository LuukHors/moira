apiVersion: moira.operator/v1alpha1
kind: AuthentikProvider
metadata:
  namespace: default
  name: authentik-provider
spec:
  baseUrl: https://auth.internal.sealeo.io
  secretRef:
    name: moira-test-oidc-credentials
    namespace: default
